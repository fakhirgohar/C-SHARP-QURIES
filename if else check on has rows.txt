if (reader.HasRows)
            {
                while (reader.Read())
                {
                    string StdId = (reader[0].ToString());
                    String StdName = (reader[1].ToString());

                    this.tbid.Text = StdId;
                    this.tbname.Text = StdName;
                    
                }
            }
            else
            {
                this.tbname.Text = "";
                MessageBox.Show("NO RECORD FOUND OF THIS ID");
               
            }